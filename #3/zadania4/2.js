// Stwórz funkcję, która wyświetli Twoje imię i nazwisko oraz informację,
// ile czasu uczysz się już programowania. Pamiętaj o poprawnym nazwaniu funkcji.

const showUserInfo = (name, surname, exp) => {
  console.log(`Imię: ${name}, Nazwisko: ${surname}, Staż: ${exp} lat`);
};

showUserInfo("Andrzej", "Kowalski", 20);

const celsiusToFahrenheit = (n) => {
  console.log(`Temperatura ${n} °C to inaczej ${(5 / 9) * n + 32} F`);
};
const fahrenheitToCelsius = (n) => {
  console.log(`Temperatura ${n} F to inaczej ${(n - 32) * (5 / 9)} °C`);
};
